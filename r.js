/* async function getDir() {
  const dirHandle = await window.showDirectoryPicker();

  // run code for dirHandle
}
getDir(); */
function rt (name,value){
  return name*value;
}
var tr = rt(5,5);
console.log(tr);